# API Reference

## Core

### `agentmesh.a2a._trace` — Trace ID Propagation

`TraceContext`, `TraceProvider`, `with_trace_context` — OpenTelemetry-compatible distributed tracing across agents.

### `agentmesh.a2a._log` — Structured Logging

`StructuredLogger`, `LogLevel`, `LoggerConfig` — JSON-format structured logging with context propagation.

### `agentmesh.a2a_provider` — A2A Provider Abstraction

`Provider` — A2A runtime abstract base class for agent communication.

## Integrations

### `agentmesh.a2a.integration.crewai_adapter` — CrewAI Adapter

Wraps AgentMesh operations as CrewAI `BaseTool` for seamless CrewAI workflow integration.

### `agentmesh.a2a.integration.autogen_adapter` — AutoGen Adapter

Wraps AgentMesh operations as AutoGen `ToolAgent` for AutoGen workflow integration.

---

See individual module pages for full class reference and usage examples.

---

## `agentmesh` 包自动生成参考

::: agentmesh
